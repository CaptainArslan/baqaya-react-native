# Screen Mapping

auth-login → app/(auth)/login.tsx  
auth-otp → app/(auth)/verify.tsx

create-shop → app/onboarding/create-shop.tsx

home-dashboard → app/(tabs)/index.tsx  
customers-list → app/(tabs)/customers.tsx  
reports → app/(tabs)/reports.tsx

add-customer → app/customers/add.tsx  
customer-details → app/customers/[id].tsx

sync-states → used across screens as components  